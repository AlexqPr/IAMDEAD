<Window x:Class="TextRedactorMVVMNew.View.Size"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextRedactorMVVMNew.View"
        mc:Ignorable="d"
        Title="Size" Height="300" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="MyText" Text="{Binding  rM, Mode=OneWayToSource}" FontSize="40"/>
        <Button Content="Подтвердить" Grid.Row="1" Command="{Binding SmenaSIZE}"/>

    </Grid>
</Window>
